{
  "name": "fluid-scale",
  "version": "0.2.0",
  "description": "A fluid interpolation engine for CSS",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marco Enrique Zimmermann",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
